# Generated results

This directory is the default destination for exhaustive startup-seed scans,
RTC mappings, and probe-analysis output. Its generated contents are ignored by
Git; only this note is versioned.

Run `tools/run_startup_seed_scan.sh` to rebuild the standard scan and RTC
mapping. The checked-in calibration input lives at
`data/btt-startup-profile.json`.
